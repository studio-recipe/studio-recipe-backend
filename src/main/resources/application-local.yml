spring:
  config:
    import: optional:file:.env[.properties]

  datasource:
#    driver-class-name: org.mariadb.jdbc.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/recipe?characterEncoding=utf8&serverTimezone=Asia/Seoul&useGssapi=false"
    username: root
    password: "tlwkrdmldkdlA!"

  jackson:
    serialization:
      write-dates-as-timestamps: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    database-platform: org.hibernate.dialect.MariaDBDialect

  mail:
    host: smtp.naver.com
    port: 465
    username: stay_on_track@naver.com
    password: KVRG8UGYM9ZJ
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      mail.smtp.timeout: 5000
      mail.smtp.connectiontimeout: 5000
      mail.smtp.writetimeout: 5000

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: dI5pBjrtgy9xFHiZtMs3fM7P8OR/wvxrexu/mybWcKc=
  access-token-validity-in-seconds: 3600
  refresh-token-validity-in-seconds: 86400

thymeleaf:
  cache: false
  prefix: classpath:/templates/
  suffix: .html

server:
  port: 8080
  servlet:
    context-path: /studio-recipe

front:
  urls: "http://localhost:5173,https://rapa8.shop,https://www.rapa8.shop"

recommender:
  flask-base-url: ${FLASK_BASE_URL:http://flask:5000}

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: TRACE
    org.springframework.security.web: TRACE

management:
  endpoints:
    web:
      exposure:
        include: "health"

